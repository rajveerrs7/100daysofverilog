`timescale 1ns / 1ps
module tb(

    );
    reg clk,reset;
    wire clkby5;
    DAY72FREQDIVIDE_5 DUT(clk, reset, clkby5);
    initial begin
    clk=1'b0;
    forever #5 clk=~clk;
    end
    initial begin
    reset = 1'b1;
    #10;
    reset = 1'b0;
    #200 $finish;
    end
    initial begin
    $dumpfile("DAY72FREQDIVIDE_5.vcd");
    $dumpvars(0,tb);
    end
endmodule
