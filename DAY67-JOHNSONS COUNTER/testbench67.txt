`timescale 1ns / 1ps
module tb(

    );
    reg clk,reset;
    wire [3:0]counter;
    DAY67JOHNSONSCOUNTER DUT(clk,reset,counter);
    initial begin
    clk=1'b0;
    forever #5 clk=~clk;
    end
    initial begin
    reset=1'b1;
    #12 reset=1'b0;
    #100 $finish;
    end
    initial begin
    $dumpfile("DAY67JOHNSONSCOUNTER.vcd");
    $dumpvars(0,tb);
    $monitor($time,"reset:%b,counter:%b",reset,counter);
    end
endmodule
